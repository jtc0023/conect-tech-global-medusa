STORE_CORS=http://localhost:8000,https://docs.medusajs.com
ADMIN_CORS=http://localhost:7000,http://localhost:7001,https://docs.medusajs.com
AUTH_CORS=http://localhost:7000,http://localhost:7001,https://docs.medusajs.com
REDIS_URL=redis://localhost:6379
JWT_SECRET=supersecret
COOKIE_SECRET=supersecret
DATABASE_URL=postgres://postgres@localhost/$DB_NAME
DB_NAME=medusa-backend

# Production additions
# Controls how Medusa runs processes: shared | server | worker
MEDUSA_WORKER_MODE=shared
# Optional separate Redis for distributed locks. If not set, falls back to REDIS_URL
LOCKING_REDIS_URL=
# Disable serving the Admin UI from the backend in production if you host it separately
DISABLE_MEDUSA_ADMIN=false